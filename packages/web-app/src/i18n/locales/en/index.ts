/**
 * The English messages, assembled from one module per feature.
 *
 * A new locale is a sibling directory with the same file names; nothing else
 * has to change.
 */

import common from './common.json'
import library from './library.json'
import pairing from './pairing.json'
import player from './player.json'
import playlists from './playlists.json'
import queue from './queue.json'
import radio from './radio.json'

const en = { common, player, queue, library, playlists, radio, pairing }

export default en
